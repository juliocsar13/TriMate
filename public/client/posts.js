const name = document.getElementsByName('name').value;
const description = document.getElementsByName('description').value;
const destination  = document.getElementsByName('destination').value;

let data = {
    name:document.getElementsByName('name').value,
    description:document.getElementsByName('description').value,
    destination:document.getElementsByName('destination').value
}
/*
fetch('/posts',data)
    .then(res=> res.)*/