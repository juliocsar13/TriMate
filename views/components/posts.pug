extends ../layout

block content
  .posts
    div(id='')
      .row
        .col-md-4
          .row
            .book-form.search-lateral
              .col-md-12
                h1(style='margin-bottom: 20px') ¡Hola, Julio! Vamos a empezar creando una publicacion.
              form(v-on='submit:addTodo')
                .col-md-12
                  label Nombre
                  input.lys-location__input(aria-label='Destino', autocomplete='off', name='')
                .col-md-12
                  label Descripcion
                  input.lys-location__input(aria-label='Destino', autocomplete='off', description='')
                .col-md-12
                  label Destino
                  input.lys-location__input(aria-label='Destino', autocomplete='off', destionation='')
                .col-md-6
                  input(type='submit', value='Crear')
        .col-md-8(style='padding: 2em 2em 7em 2em;')
          .row
            .col-md-12
              | {{#each posts}}     
              .col-md-6
                img.img-responsive(src='images/about1.jpg', alt='')
                h4 {{name}}
                p  {{description}}
                div
                  button.btn.btn-primary Ver mas
              |                         {{/each}}
